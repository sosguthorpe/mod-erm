import org.olf.kb.TitleInstance

import groovy.transform.*

@Field
TitleInstance titleInstance

json g.render(titleInstance, [expand: ['identifiers']]) {

  // Add the custom summary option
  "entitlements" g.render( titleInstance.getEntitlements() )

  // entitlementsTwo title.getEntitlements()
}