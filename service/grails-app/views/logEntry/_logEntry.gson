import groovy.transform.Field
import org.olf.general.jobs.LogEntry

@Field
LogEntry logEntry

json g.render(logEntry, [include: ['message', 'type', 'additionalInfo']]){
  additionalinfo g.render(logEntry.additionalInfo)
}
