import groovy.transform.*
import org.olf.general.RefdataValue

@Field RefdataValue refdataValue

json {
  id refdataValue.id
  value refdataValue.value
  label refdataValue.label
//  g.render(refdataValue, [expand: ['owner']])
}