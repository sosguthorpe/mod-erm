#!/bin/bash -e
assemble
rc=$?

if [ $rc -eq 0 ]; then
  echo " --> Moving okapi resources"
  mv /tmp/src/build/resources/okapi ~/app-root/okapi
  
else
  echo " --> After failed assembling"
fi

exit $rc